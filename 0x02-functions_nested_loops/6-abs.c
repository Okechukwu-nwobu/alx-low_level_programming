#include "main.h"

/**
 * _abs - This computes the absolute value of an integer.
 *@n: The parameter to be tested.
 *
 *Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
