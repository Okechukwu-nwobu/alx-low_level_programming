#include <stdio.h>

/**
 * main - This is the entry point.
 * printf: This prints stringed output
 *
 * Description: This prints a string without an escape to a new line.
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
